<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SafeJourney</title>
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<main>
  <section id="section2" class="position-relative text-center">
    <%= image_tag("bus1.jpg", class: "img-fluid", alt: "Your Image") %>
    <div class="search-bar-overlay position-absolute top-50 start-50 translate-middle">
      <div class="container mt-5">
        <div class="row justify-content-center">
          <div class="col-md-6" style="border-radius: 10px; width: 90%;">
            <!-- Use form_tag to create the outer form -->
            <%= form_tag(buses_path, method: "get", class: "mb-4", id: "search-form") do %>
              <div class="input-group">
                <%= text_field_tag(:from, params[:from], class: "form-control rounded-0", placeholder: "From", "aria-label": "select location", required: true) %>
                <%= text_field_tag(:to, params[:to], class: "form-control rounded-0", placeholder: "To", "aria-label": "select location", required: true) %>
                <%= date_field_tag(:book_date, params[:book_date], class: "form-control rounded-0", placeholder: "Book Date", "aria-label": "Book Date", required: true) %>
                <!-- your existing form fields here -->
                <button type="submit" class="btn btn-primary rounded-0">Search</button>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </section>
</main>
</body>
</html>
